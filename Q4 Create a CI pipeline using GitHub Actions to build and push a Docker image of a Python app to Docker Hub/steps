✅ Step 1 – Create Project Files

Inside your project folder, create:

🔹 app.py
from flask import Flask
app = Flask(__name__)

@app.route('/')
def home():
    return "Hello from CI Pipeline!"

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
🔹 requirements.txt
flask
🔹 Dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 5000

CMD ["python", "app.py"]
✅ Step 2 – Initialize Git & Push to GitHub
git init
git add .
git commit -m "Initial commit"

git remote add origin https://github.com/<username>/<repo>.git
git branch -M main

# Important fix
git pull origin main --allow-unrelated-histories

git push -u origin main
✅ Step 3 – Add GitHub Secrets 🔐

Go to your GitHub repo:

👉 Settings → Secrets and variables → Actions → New repository secret

Add:

Name: DOCKER_USERNAME → devopslab1218
Name: DOCKER_PASSWORD → (Docker Hub Access Token)

⚠️ Use access token, not password

✅ Step 4 – Create Workflow File

Create folder:

.github/workflows/

Create file:

docker-publish.yml
✅ Step 5 – Add Workflow Code
name: Build and Push Docker Image

on:
  push:
    branches: [ main ]

jobs:
  build-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/python-app:latest .

      - name: Push to Docker Hub
        run: docker push ${{ secrets.DOCKER_USERNAME }}/python-app:latest
✅ Step 6 – Push Workflow to GitHub
git add .
git commit -m "Add CI pipeline"
git push
✅ Step 7 – Verify Execution
Go to GitHub → Actions tab
Open workflow → Build and Push Docker Image
Check:
✔️ Build step success
✔️ Push step success
✅ Step 8 – Check Docker Hub

Go to Docker Hub → repository:

devopslab1218/python-app

👉 You will see:

latest
